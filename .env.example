# Redis 配置
REDIS_HOST=localhost
# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=123456
REDIS_DB=1

# 日志配置
# LOG_LEVEL 支持名称或数字（CRITICAL/ERROR/WARNING/INFO/DEBUG/NOTSET 或 50/40/30/20/10/0）
# 示例：LOG_LEVEL=INFO
LOG_LEVEL=DEBUG

# zookeeper 配置
ZOOKEEPER_HOSTS=127.0.0.1:2181

# Mysql 配置
DB_HOST=172.27.54.184
DB_USER=root
DB_PASSWORD=123456
DB_NAME=test
DB_PORT=33061

# 百度云API KEY
BAIDUYUN_API_KEY=bce-v3/ALTAK-xxxx/xxxx

# JWT 配置（用于认证）
JWT_SECRET=your-strong-secret
JWT_ISS=KairoCore
JWT_AUD=KairoCoreClients
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=1209600

# API_KEY 支持（永久有效的服务访问密钥）
# 1) 直接在环境变量中提供 API_KEY（优先级更高），用于快速开发或简单部署
#    注意：生产环境请使用文件方式或安全的密钥管理系统，不要将真实密钥提交到代码仓库
KC_API_KEY=your-api-key-here

# 2) 从文件中读取 API_KEY（如果 KC_API_KEY 未设置则回退到此），适合生产环境
#    文件内容只需包含一行密钥文本，KairoAuth 将按此文件路径读取（默认 ~/.kairocore/api.key）
KC_API_KEY_FILE=/path/to/your/api.key

# 登录密码加密上传与严格模式（可选）
# rsa：启用 RSA-OAEP(SHA-256) 解密；none：不启用加密；aes：使用共享密钥 AES-GCM
LOGIN_PASSWORD_ENCRYPTION=rsa
# 要求前端必须加密上传密码；true 时解密失败或未加密将直接拒绝登录
LOGIN_PASSWORD_REQUIRE_ENCRYPTION=true
# RSA 私钥的提供方式（二选一）
AUTH_RSA_PRIVATE_KEY_FILE=/path/to/private_key.pem
# 或：将 PEM 内容直接放在环境变量中（不推荐生产）
# AUTH_RSA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# 若私钥带口令，则配置此项
AUTH_RSA_PRIVATE_KEY_PASSPHRASE=your-passphrase
# AES-GCM 共享密钥模式（单一 secret_key，不使用公私钥）
# 将 Base64 编码的 16/24/32 字节密钥写入此变量（对应 AES-128/192/256）
# 示例：LOGIN_PASSWORD_SECRET_KEY=base64_of_32_bytes_key
# 当 LOGIN_PASSWORD_ENCRYPTION=aes 或前端以 "aes:" 前缀提交密码时启用
LOGIN_PASSWORD_SECRET_KEY=aes

# 管理路由开关：是否启用 API_KEY 管理端点（生成/删除）
# - 开发环境建议开启（true），便于本地生成和测试
# - 生产环境建议关闭（false），或仅在管理员角色下访问
KC_ENABLE_API_KEY_ADMIN=false

# 应用环境标识：用于决定 API_KEY 管理路由的访问策略
# - development：本地开发模式，允许免登录访问管理端点（配合 KC_ENABLE_API_KEY_ADMIN=true）
# - production：生产模式，仅管理员角色可访问管理端点
KC_ENV=development

# 微信公众号认证配置
# 微信公众号认证 token，用于验证请求来源
WX_GZH_TOKEN=5dictSupplier
# 微信公众号认证 EncodingAESKey，用于消息加密
WX_GZH_ENCODING_AES_KEY=FPWw3Glh7iZQwZOxmTBDBH6wNW206diXJtDYt5Pq5US
# 微信公众号APPID
WX_GZH_APPID=wxc2bd1b022dc5bc1d
# 微信公众号APPSECRET
WX_GZH_APPSECRET=60b4895ca72663c41083a00726166914
# 微信对接 Dify 配置
WX_DIFY_API_URL=http://36.134.168.150:8098/v1/chat-messages
WX_DIFY_API_KEY=app-OtBnvqUtedwztbpUSLmmYdj8

# RabbitMQ 配置（用于在线状态检测、心跳与 RPC ping）
# 主机与端口
RABBITMQ_HOST=127.0.0.1
RABBITMQ_PORT=5672
# 认证信息（开发环境默认 guest/guest，生产请改为安全账号）
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
# 虚拟主机（默认 /）
RABBITMQ_VHOST=/
# 心跳间隔（秒），用于连接保活，默认 30
RABBITMQ_HEARTBEAT=30
